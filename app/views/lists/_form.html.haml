= form_for(@list) do |f|
  .modal-body
    .field
      = f.label :name
      = f.text_field :name
  .modal-footer
    %button.btn{ "data-dismiss" => "modal", "aria-hidden" => "true"} Close
    %button.btn.btn-primary{ :type => :submit } Save
